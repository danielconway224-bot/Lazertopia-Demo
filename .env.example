# Copy to ".env" if you need to change anything. Nothing here is required —
# the demo runs with no configuration at all.

# Port the local server listens on (default 4242).
PORT=4242

# ── Not wired up yet ──
# Payments and the database are deliberately out of scope for this stage.
# When we add them, the keys will go here (and ".env" stays gitignored):
#   STRIPE_SECRET_KEY / STRIPE_PUBLISHABLE_KEY   — test keys only in a prototype
#   SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY
